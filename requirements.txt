fastapi>=0.110.0
uvicorn[standard]>=0.27.0
itsdangerous>=2.1.0
python-multipart>=0.0.9
Pillow>=10.0.0
anthropic>=0.40.0
google-genai>=0.8.0
groq>=0.11.0
python-dotenv>=1.0.0
# F1 normalization (background removal at ingest). rembg[cpu] pulls onnxruntime,
# numpy, scipy, scikit-image, numba — total install ~300-400MB on disk.
# First run downloads the u2net model (~176MB) to ~/.u2net/; guarded for
# offline/failure with an explicit FAILED status in normalize.py.
rembg[cpu]>=2.0.50
onnxruntime>=1.16.0

# PSD por capas para diseñadores (exportador editable). six es dependencia de pytoshop.
pytoshop>=1.2.0
six>=1.16.0
